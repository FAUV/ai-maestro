# Design System

This folder documents the semantic layers used by the studio pages:

- `tokens/` for color, spacing, typography, and grid primitives.
- `motion-system/` for duration, easing, and spring behavior.
- `components/ui` and `components/glass` for reusable building blocks.
